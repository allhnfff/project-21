version: "3.9"

services:
  app:
    build: .

    container_name: htstore-app

    restart: always

    ports:
      - "3000:3000"

    environment:
      NODE_ENV: production

    depends_on:
      - postgres

  postgres:
    image: postgres:16-alpine

    container_name: htstore-db

    restart: always

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: htstore

    ports:
      - "5432:5432"

    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: